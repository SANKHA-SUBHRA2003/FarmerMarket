name: your_project_name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you are planning to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.20
  intl: ^0.19.0
  file_picker: ^8.0.6
  cupertino_icons: ^1.0.2
  # webview_flutter is not recommended for Flutter web
  # Remove webview_flutter if it’s causing issues

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
